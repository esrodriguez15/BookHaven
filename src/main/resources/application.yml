server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:mysql://google/bookapp?cloudSqlInstance=bookhaven-491802:us-central1:bookhaven&socketFactory=com.google.cloud.sql.mysql.SocketFactory&useSSL=false
    username: rootroot
    password: root1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

management:
  endpoints:
    web:
      exposure:
        include: health,info